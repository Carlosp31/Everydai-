name: deploy
on:
  push:
    branches:
      - main

jobs:
  deploy_carlos:
    runs-on: carlosp
    steps:
      - uses: actions/checkout@v2
      - name: Copy .env
        run: cp ~/varnen/.env .
      - name: Copy venv
        run: cp ~/varnen/venv ~/.
